<% @users.each do |e| %>
  <p>
    <%= e.uid %>
    <%= e.pass %>
    <%= link_to '削除', user_path(e), method: :delete %>
    </p>
<% end %>

<p>
  <%= link_to 'アカウント作成', new_user_path %>
</p>

<p>
  <%= link_to '近くの飲食店一覧', tweets_path %>
</p>
